<% layout('../layouts/content') -%>

<% script(`/${module}/js/activity/index.js`) -%>

<div class="box">
  <div class="list-loader overlay" style="display: none;">
    <i class="fa fa-refresh fa-spin"></i>
  </div>
  <div class="box-body">
    <div class="list-tools">
      <div class="row">

        <div class="col-lg-2">
          <div class="form-group filter-form-group">
            <label><%=t('Period')%></label>
            <input type="text" class="period-filter form-control">
          </div>
        </div>

        <div class="col-lg-3">
          <div class="form-group">
            <label><%=t('User')%></label>
            <select class="user-filter form-control">
              <option value="">-</option>
                <% for (let user of users) { %>
                  <option value="<%- user.login() %>">
                    <%- user.name() %>
                  </option>
                <% } %>
            </select>
          </div>
        </div>

        <div class="col-lg-2">
          <div class="form-group">
            <label><%=t('Disabled')%></label>
            <select class="disabled-filter form-control">
              <option value="" selected><%=t('All')%></option>
              <option value="false"><%=t('Only allowed ones')%></option>
              <option value="true"><%=t('Only blocked ones')%></option>
            </select>
          </div>
        </div>
      </div>

      <div>
        <button class="filter btn btn-primary"><%=t('Filter')%></button>
        <button class="reset-filter btn btn-default"><%=t('Reset filter')%></button>
      </div>
    </div>

    <div class="table-responsive">
      <table class="table table-bordered table-striped multi-selectable activity-table">
        <thead>
        <tr></tr>
        </thead>
      </table>
    </div>
  </div>
</div>